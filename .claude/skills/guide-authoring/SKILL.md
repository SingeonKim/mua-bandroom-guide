---
name: guide-authoring
description: 합주실 일반 이용자용 사용 가이드(docs/guide.md)와 랜딩(docs/index.md)을 작성/갱신할 때 사용. spec의 확정 정보만으로 비전문가가 따라 할 수 있게 짧고 쉽게 쓰고, 잠정값·게인 수치는 절대 넣지 않는다. "사용 가이드 작성/갱신", "guide.md 만들어", "이용자용 안내", "랜딩 페이지" 작업에 반드시 사용할 것.
---

# guide-authoring — 이용자용 가이드 작성

믹서를 모르는 이용자가 합주 직전에 급히 보는 안내를 쓴다. 메시지는 하나: **"씬 불러오기 + 본인 자리에 꽂기"만 하면 된다.**

## 1순위 규칙: 잠정값·게인 수치 금지

이용자는 검증 안 된 값을 알 필요가 없고, 잘못 따라 하면 세팅이 꼬인다. 그래서:

- **spec-editor의 "확정 정보 목록"에 있는 것만 쓴다.** 목록에 없으면 안 쓴다.
- 구체적 dB 게인, 컴프/게이트/EQ 파라미터, 모니터 분배 수치는 **넣지 않는다**.
- 확정 여부가 애매하면 **빼는 쪽**을 택한다(누락이 누출보다 안전).

이게 guide-writer와 다른 작성자의 핵심 차이다 — 정확성보다 **안전한 정보 필터링**이 먼저다.

## 쉽게 쓰는 법

- 전문용어 금지: "Pre-fader Bus" → "본인 자리 앞 바닥 스피커". "48V" → (아예 언급 안 함, 건드리지 말라고만)
- 명령형 단문, 번호 단계, 표 1~2개. 한 화면에 들어오게.
- 스크린샷이 필요한 곳은 `![씬 불러오기 화면](img/scene-load.png)` 자리표시자를 남긴다.

## guide.md 골격

```
1. 시작 전 1분    — 게인/라우팅 건드리지 말 것 + 끝나면 씬 다시 불러오기
2. 씬 불러오는 법  — X32 기본 씬 로드 단계 (순서, 스크린샷 자리)
3. 내 악기 어디 꽂나 — 표준 배치표(확정 ch 매핑) + 본인 모니터 위치
4. 외부 밴드라면   — 최소 응용 1섹션 (기타는 앰프 인풋 / 재배치는 관리자에게)
5. 문제 생기면     — 빠른 체크리스트(뮤트? 페이더? 씬 다시 로드) + 관리자 연락
6. 하지 말 것      — !!! danger 박스 (게인/라우팅/48V 금지)
```

## index.md 골격

- 한 줄 소개: "MUA 합주실 PA 안내"
- 갈림길 2개: ［처음 오셨나요 → 사용 가이드］ / ［세팅 관리 → 스펙 문서］ (상대 링크 `guide.md`, `spec.md`)
- 장비 한 줄: X32 Producer / DHR12M ×5 / PRX ONE ×2

## admonition 사용

- 금지 사항: `!!! danger "건드리지 마세요"`
- 도움 팁: `!!! tip`
- 주의: `!!! warning` (단, 잠정값을 넣으라는 뜻이 아님 — 운영상 주의만)

## 갱신 모드 (재호출)

- 기존 `guide.md`/`index.md`가 있으면 읽어서 갱신.
- spec에서 잠정→확정된 값이 와도, **이용자에게 실제로 도움이 될 때만** 추가한다. 대부분의 게인 수치는 확정돼도 guide엔 불필요("건드리지 마라" 유지).

## 외부 밴드 섹션 범위

가볍게 1섹션. "기타는 앰프 인풋에 꽂으면 됩니다 / 자리나 게인을 바꿔야 하면 직접 만지지 말고 관리자에게 연락하세요" 수준. 복잡한 연결 케이스 분기는 spec으로 안내(링크).
